#include<stdio.h>
int main ()
{
    int n,row,col;
    printf("Enter N :");
    scanf("%d",&n);

    for (row=1 ;row<=n ;row++)
    {
        //1st loop ER 1st MAAN ER JONNO 2nd loop (n) PORJONTO run hoy:

        for (col=1 ;col<=n; col++)
        {
            printf("*");
        }
            printf("\n");

    }
return 0;
}
